import React from 'react';
import {
    View,
    Text,
} from 'react-native';

const FavoritePlaceScreen = ({navigation}) => {

    return(
        <View>
            <Text>FavoritePlace Screen</Text>
        </View>

    );
};

export default FavoritePlaceScreen;